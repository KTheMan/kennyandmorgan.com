name: Deploy GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Convert non-PNG images to PNG
        run: |
          python -m pip install --disable-pip-version-check Pillow==12.2.0 pillow-heif==1.3.0
          python - <<'PY'
          import shutil
          import tempfile
          from pathlib import Path

          from PIL import Image, ImageOps
          from pillow_heif import register_heif_opener

          register_heif_opener()

          repo_root = Path.cwd()
          images_dir = repo_root / 'images'
          normalized_dir = Path(tempfile.gettempdir()) / 'km-normalized-images'
          source_extensions = {
              '.jpg',
              '.jpeg',
              '.jpe',
              '.jfif',
              '.heic',
              '.heif',
              '.avif',
              '.webp',
              '.gif',
              '.bmp',
              '.dib',
              '.tif',
              '.tiff',
          }

          if normalized_dir.exists():
              shutil.rmtree(normalized_dir)

          source_files = sorted(
              path for path in images_dir.iterdir()
              if path.is_file() and path.suffix.lower() in source_extensions
          )
          if not source_files:
              print('No non-PNG source images detected in images/.')
              raise SystemExit(0)

          normalized_dir.mkdir(parents=True, exist_ok=True)

          for source_path in source_files:
              target_path = normalized_dir / f'{source_path.stem}.png'
              with Image.open(source_path) as image:
                  normalized = ImageOps.exif_transpose(image)
                  alpha_in_mode = 'A' in normalized.getbands()
                  alpha_in_info = bool(normalized.info.get('transparency'))
                  has_alpha = alpha_in_mode or alpha_in_info
                  if normalized.mode not in ('RGB', 'RGBA'):
                      normalized = normalized.convert('RGBA' if has_alpha else 'RGB')
                  normalized.save(target_path, format='PNG', compress_level=9)
              print(f'Converted {source_path.name} -> {target_path.name}')
          PY
      - uses: actions/configure-pages@v5
      - name: Prepare deploy artifact
        run: |
          mkdir -p dist
          cp index.html admin.html script.js admin.js styles.css site-config.js data-client.js .nojekyll dist/
          cp -R images dist/images
          if [ -d /tmp/km-normalized-images ]; then
            cp /tmp/km-normalized-images/*.png dist/images/
          fi
          cp -R vendor dist/vendor
      - name: Generate runtime config
        env:
          SITE_REGISTRY_PAGE_URL: ${{ vars.SITE_REGISTRY_PAGE_URL }}
          SITE_SUPABASE_URL: ${{ vars.SITE_SUPABASE_URL }}
          SITE_SUPABASE_ANON_KEY: ${{ secrets.SITE_SUPABASE_ANON_KEY }}
          SITE_SUPABASE_SESSION_TTL_MS: ${{ vars.SITE_SUPABASE_SESSION_TTL_MS || '3600000' }}
        run: |
          python - <<'PY'
          import json
          import os
          import sys

          required = (
              'SITE_REGISTRY_PAGE_URL',
              'SITE_SUPABASE_URL',
              'SITE_SUPABASE_ANON_KEY',
          )
          missing = [name for name in required if not os.environ.get(name)]
          if missing:
              sys.stderr.write(
                  'Missing required GitHub Actions configuration: '
                  + ', '.join(missing)
                  + '\n'
              )
              raise SystemExit(1)

          try:
              session_ttl_ms = int(os.environ.get('SITE_SUPABASE_SESSION_TTL_MS', '3600000'))
          except ValueError as err:
              sys.stderr.write(f'Invalid SITE_SUPABASE_SESSION_TTL_MS: {err}\n')
              raise SystemExit(1)

          if session_ttl_ms <= 0:
              sys.stderr.write('SITE_SUPABASE_SESSION_TTL_MS must be greater than 0.\n')
              raise SystemExit(1)

          config = {
              'registryPageUrl': os.environ['SITE_REGISTRY_PAGE_URL'],
              'supabase': {
                  'url': os.environ['SITE_SUPABASE_URL'],
                  'anonKey': os.environ['SITE_SUPABASE_ANON_KEY'],
                  'sessionTtlMs': session_ttl_ms,
              },
              'localFallbackAccess': {
                  'familyPassword': '',
                  'partyPassword': '',
                  'adminPassword': '',
              },
          }

          with open('dist/site.config.json', 'w', encoding='utf-8') as file:
              json.dump(config, file, indent=2)
              file.write('\n')
          PY
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
